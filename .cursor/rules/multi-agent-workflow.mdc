---
description: 多 Agent 协同工作流 — 触发条件、角色定义、日志文件系统。当用户输入 /multi-agent 或要求多 agent 协同工作时使用。
alwaysApply: true
---

# Multi-Agent 协同工作流

## 触发条件

当用户输入 `/multi-agent` 或明确要求多 agent 协同工作时，按本规则执行完整工作流。

## Cursor 适配说明

本工作流基于 Cursor Agent 的 `Task` 工具 dispatch subagent：
- **Master**：当前 Cursor Agent 自身，负责调度和最终裁决
- **Subagent**：通过 `Task` 工具 dispatch，类型选择 `generalPurpose`
- **通信方式**：所有 Agent 通过**共享日志文件**异步通信（写入自己的日志，读取协作方日志）
- Master 不做信息搬运，只负责调度时机和最终裁决

## 核心约束

- Subagent 是无状态独立进程，不共享上下文
- Agent 间通信必须通过日志文件，不经过 Master 中转
- 可并行的阶段必须在**同一条消息**中发起多个 Task 工具调用
- 每个反馈循环有最大轮次限制，超限后 Master 做最终裁决

---

## Agent 角色定义（7 个角色）

| 角色 | 类型 | 职责 |
|------|------|------|
| **Master** | 主 Agent（Cursor 自身） | 接收需求、调度 subagent、最终裁决和呈现 |
| **ProjectManager** | subagent | 里程碑制定、风险跟踪、可行性评估、版本管理、Release Note、产品使用说明书 |
| **TechLeader** | subagent | 技术调研、架构设计、模块划分 |
| **Developer** | subagent（可多个并行） | 具体编码实现、Bug 修复 |
| **CodeReviewer** | subagent | 代码审查、问题分级（P0/P1/P2） |
| **Tester** | subagent | 编写测试、执行验证 |
| **Customer** | subagent | 理解需求、验收交付结果（必须实际运行程序） |

## Agent 协作关系（通过日志文件直接通信）

| 协作对 | 通信方式 |
|--------|----------|
| TechLeader ↔ ProjectManager | 互读日志（PM 读 TL 计划；TL 读 PM 反馈） |
| Developer ↔ CodeReviewer | 互读日志（CR 读 Dev 代码；Dev 读 CR 审查） |
| Developer ↔ Tester | 互读日志（Tester 读 Dev 代码；Dev 读 Tester 失败报告） |
| Customer ← ProjectManager | Customer 读取 PM 日志进行验收 |

---

## 日志文件系统

### 目录结构

```
.cursor/agents/
├── v0.1.0/                          # 首轮开发周期
│   ├── tech-leader.log.md
│   ├── project-manager.log.md
│   ├── developer-{module-name}.log.md
│   ├── code-reviewer.log.md
│   ├── tester.log.md
│   └── customer.log.md
├── v0.1.1/                          # 回归开发周期（如有）
│   └── ...
└── ...
```

版本目录命名：`v{major}.{minor}.{patch}`，首轮默认 `v0.1.0`。

### 日志读写规则

| 角色 | 写入 | 读取 |
|------|------|------|
| Master | 无（通过 prompt 传递需求） | 所有版本目录下的日志 |
| TechLeader | `{ver}/tech-leader.log.md` | `{ver}/project-manager.log.md` |
| ProjectManager | `{ver}/project-manager.log.md` | `{ver}/tech-leader.log.md` |
| Developer | `{ver}/developer-{module}.log.md` | `{ver}/tech-leader.log.md`、`{ver}/code-reviewer.log.md`、`{ver}/tester.log.md` |
| CodeReviewer | `{ver}/code-reviewer.log.md` | `{ver}/developer-*.log.md` |
| Tester | `{ver}/tester.log.md` | `{ver}/developer-*.log.md` |
| Customer | `{ver}/customer.log.md` | `{ver}/project-manager.log.md` |

### 日志条目格式

每个 Agent 追加如下格式的条目到日志文件：

```markdown
## [{唯一ID}] {任务标题}

- **阶段：** {所属阶段编号}
- **状态：** pending | in_progress | completed
- **时间：** {ISO 时间戳}

### 输入
{本次任务接收的输入摘要}

### 输出
{本次任务的输出内容}

---
```

唯一 ID 规则：`{角色缩写}-{阶段号}-{序号}`，如 `TL-2-1`、`DEV-4-2`、`CR-5-1`。

---

## 并行策略速查

| 阶段 | 策略 | 说明 |
|------|------|------|
| 阶段 2 | **并行** | TechLeader 和 ProjectManager 同时 dispatch |
| 阶段 3 | 串行循环 | PM → TL → PM（最多 2 轮） |
| 阶段 4 | **按依赖并行** | 独立模块并行，有依赖的串行 |
| 阶段 5 | 串行循环 | Developer ↔ CodeReviewer（最多 3 轮） |
| 阶段 6 | 串行循环 | Developer ↔ Tester（最多 2 轮） |
| 阶段 7 | 串行 | PM 先确认版本号，Customer 再验收 |
| 阶段 8 | 条件触发 | Customer 不通过时回到阶段 2（最多 1 次） |
| 阶段 9 | **并行** | TechLeader 和 ProjectManager 同时 dispatch |

详细阶段说明见 `.cursor/rules/multi-agent-phases.mdc`。
子 Agent Prompt 模板见 `.cursor/rules/multi-agent-prompts.mdc`。
